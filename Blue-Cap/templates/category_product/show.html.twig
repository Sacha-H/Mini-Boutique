{% extends 'base.html.twig' %}

{% block title %}CategoryProduct{% endblock %}

{% block body %}
    <h1>CategoryProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_product.id }}</td>
            </tr>
            <tr>
                <th>NameCategoryProduct</th>
                <td>{{ category_product.nameCategoryProduct }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_product_index') }}">back to list</a>

    <a href="{{ path('category_product_edit', {'id': category_product.id}) }}">edit</a>

    {{ include('category_product/_delete_form.html.twig') }}
{% endblock %}
